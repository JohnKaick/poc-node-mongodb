const _ = require('lodash')
const eh = require('common/error-handler')
const db = require('database')
const { IdeEmpresa, IdeUsuario, IdePermissao } = db.models

export default async function (empresaId) {

}